export const goToHome = (navigator) => {
    navigator('/');
};

export const goToProjects = (navigator) => {
    navigator('/projects');
};

export const goToAbout = (navigator) => {
    navigator('/about');
};

export const goToContact = (navigator) => {
    navigator('/contact');
};
